# FileForge Frontend Environment Configuration

# Backend API base URL (required for production)
# Example for AWS Lambda + API Gateway:
VITE_API_BASE_URL=https://abc123.execute-api.us-east-1.amazonaws.com

# For local development with a proxy, you can use:
# VITE_API_BASE_URL=http://localhost:3000
